 du coup sur le compte. Amazon EC2, donc c'est la C-Cloud Compute. Donc il fournit une capacité de calcul sécurisé et redimensionnable sur le cloud. C'est-à-dire voilà, j'ai pu changer de capacité de cette machine que je choisis, cette l'AVM que je choisis, et les destinées aux developers. Donc à travers l'interface web du service ou bien la console Amazon, je peux obtenir et configurer des capacités et voilà je peux aussi monitorer et après c'est une VM à laquelle je peux me connecter aux necessary services et effectuer toutes les opérations que je souhaite faire. Alors le modèle de tarification c'est alors, après il y a le instance spot, c'est des instances qui sont particulières avec des capacités de calcul qu'on peut ajuster, après il y a les instances réservées, je peux avoir une instance réservée, c'est-à-dire ça c'est pas fait pour les premiers défis par exemple, c'est fait pour les choses qui sont à long terme dans lesquelles on s'engage entre un an et trois ans, ça permet de réduire le coût. Lorsqu'on sait qu'on a une VM dont on a besoin pendant l'entran, on peut prendre une instance avec engagement et ça permet de réduire le coût, après il y a les autres dédiés. Là c'est pas seulement que je réserve l'AVM, c'est que je réserve aussi les ressources physiques, c'est-à-dire je demande à Amazon de me garantir les mêmes ressources physiques en fait. Alors il y a plein de types d'instances, ils sont categorisés par famille et ça c'est en fonction de notre uscée, ça dépend qu'est ce qu'on va faire avec cette VM, il y a des VM qui sont voilà pour les dev, il y a des VVM sur lesquelles ils sont adaptés pour les bases données, il y a des VM qui sont faites pour les applications de traitement massif de données, de streaming, il y a des VM aussi, voilà pour le machine learning par exemple qui nécessite beaucoup plus de CPU. Donc ça dépend de notre uscée et en fonction de ce uscée là on peut choisir la catégorie de VM. Après, alors chaque VM on peut lui attribuer des groupes de sécurité, les groupes de sécurité finalement c'est des firewalls, c'est des firewalls virtuels, ça permet la communication des VM entre des ports spécifiques, par défaut tous les flux entrants sont fermés et tous les flux sortants sont au baird, de façon générale c'est suffisant de configurer les flux entrants parce que les risques il est par rapport à tout ce qui entre, tout ce qui entre principalement et lorsqu'on fait un changement de règle ça prend effet immédiatement. Donc c'est les groupes de sécurité, ça permet de définir des règles du coup d'ouverture de flux entre les ports, entre les différents sens et pour avoir plusieurs sens EC2 sans un groupe de sécurité on peut aussi attribuer un groupe de sécurité à plusieurs sens EC2 et on peut attribuer plusieurs sens EC2 pour le même groupe de sécurité, c'est une relation end-to-end. Alors lorsqu'on veut démarrer une VM, obligatoirement on a besoin de disques en fait, on a vu le disque de disques au moins un disque qui contient le disque sur lequel on va goûter, qui contient le système d'exploitation et un suite suite. Donc voilà les disques sur AWS c'est un autre service qui s'appelle Amazon OBS et là c'est que bloc store, c'est quoi ? C'est une disque virtuelle, chaque volume EBS est automatiquement répliqué au sein de availability zone, ce sont des disques virtuels du coup qui sont répliqués au sein de la même zone de disponibilité, il y a toujours une copie, plusieurs types et avec des caractéristiques différentes donc il y a plusieurs types de disques finalement, on sait tout ce qu'il y a des disques HDD, des disques SSD, des disques magnétiques, ce sont des générations de disques, au début s'il y avait des disques magnétiques, après il y a eu des optimisations et après il y a des disques SSD maintenant qui sont beaucoup plus performantes particulièrement lorsqu'il y a des écritures irrugulaires. Donc pour chacun de ces disques là, pour chacun de ces catégories de disques, il y a des des performances. Donc voilà pour les magnétiques c'est jusqu'à IOPS, c'est le nombre de input ou bien output par seconde, le nombre de lecture et créature par seconde. Alors pour les disques magnétiques c'est de l'ordre de 40 jusqu'à 200 et on peut aller avec les meilleures des SSD jusqu'à 64000 lecture et créature. Alors imaginez qu'on va travailler ensemble sur un produit. Moi j'ai créé une VM, j'ai installé des choses, j'ai trouvé déjà par exemple une VM au bentou, j'ai installé d'autres logiciels, je sais pas un Cassandra, un Oracle et un Intelligi pour travailler et après je veux que je veux éviter que chacun de vous il fait la même chose, pour ne pas perdre du temps. Alors qu'est ce qu'on peut faire ? C'est quelque chose qui est possible pour dans AWS, c'est de en quelque sorte cloner le disque. Alors le clone de disque ça passe par le créer une snapshot et à partir de cette snapshot on peut instanter plusieurs UBS. Donc l'idée si moi je veux partager mon disque qui contient des données qui nous intéressent tous, ce que je peux faire c'est que je crée un snapshot de mon disque après avoir tout installé et après je vous demande à chacun, je vous donne l'autorisation à chacun de créer une instance AWS à partir de ce disque et là on aura tous un disque, les mêmes données dont le disque dure. Ok, ça évite que chacun il va faire les installations dans son coin. Les snapshots sont stockés dans S3, ils sont incrementals aussi, c'est versionné. Oui, une recommandation c'est qu'avant de faire un snapshot d'une UBS route, ce qui contient une VM, ce qui contient un OS, qui contient un système d'exploitation, un Ubuntu, un Windows, il vaut mieux l'arrêter avant le système d'exploitation. Il vaut mieux ne pas mettre le BSU route qui permet de beauté, qui contient le système d'exploitation et tout ça. Il vaut mieux, pour faire un snapshot de ça, il vaut mieux arrêter le EC2 avant de faire le snapshot. D'accord, l'histoire de se retrouver dans un état stable. Alors il est possible de créer une AMI à partir d'un snapshot. Alors c'est quoi l'AMI ? L'AMI, on va toujours supposer le cas où j'ai fait une installation d'un système d'exploitation, j'ai installé beaucoup de choses sur la même disque et après je vous partageais ça. Sauf que vous n'avez pas besoin d'un EBS, vous avez besoin d'une machine virtuelle EC2. On voulait avoir une machine virtuelle EC2 à partir de cet EBS. Si vous faites une instantiation directement à partir de snapshot, vous allez retrouver juste un EBS. Si vous voulez par contre créer une VM à partir de ce snapshot, il suffit juste de créer une AMI à partir de ce snapshot. Alors du coup l'AMI c'est quoi ? C'est juste une image d'une machine virtuelle, une image d'une EC2. Donc l'image d'un EBS ça s'appelle le snapshot, l'image d'un EC2 ça s'appelle AMI, c'est aussi simple que ça. Donc voilà,